spring:
    data:
      mongodb:
        uri: ${MONGODB_URI}
        database: ${MONGODB_DATABASE}
        auto-index-creation: true

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO

frontend:
  url: ${FRONTEND_URL}
